2024-01-06  Georgios Athanasiou  <yorgath@gmail.com>

	* scm/yamlpp.scm
	(yaml-load-node): New primitive wrapper.
	(yaml-load-node-from-file): New primitive wrapper.
	(yaml-load-nodes): New primitive wrapper.
	(yaml-load-nodes-from-file): New primitive wrapper.
	(yaml-node->alist): New primitive wrapper.
	(yaml-node->list): New primitive wrapper.
	(yaml-node-type): New primitive wrapper.
	(yaml-scalar-value): New primitive wrapper.
	(yaml-set-indent!): New primitive wrapper.
	(yaml-set-map-format-1!): New primitive wrapper.
	(yaml-set-seq-format-1!): New primitive wrapper.

2024-01-05  Georgios Athanasiou  <yorgath@gmail.com>

	* scm/yamlpp.scm
	(make-yaml-emitter): New primitive wrapper.
	(yaml-emitter-good?): New primitive wrapper.
	(yaml-emitter-string): New primitive wrapper.
	(yaml-emit-null!): New primitive wrapper.
	(yaml-emit-string!): New primitive wrapper.
	(yaml-emit-boolean!): New primitive wrapper.
	(yaml-emit-integer!): New primitive wrapper.
	(yaml-emit-comment!): New primitive wrapper.
	(yaml-emit-newline!): New primitive wrapper.
	(yaml-begin-doc!): New primitive wrapper.
	(yaml-end-doc!): New primitive wrapper.
	(yaml-begin-seq!): New primitive wrapper.
	(yaml-end-seq!): New primitive wrapper.
	(yaml-begin-map!): New primitive wrapper.
	(yaml-end-map!): New primitive wrapper.
	(yaml-emit-key!): New primitive wrapper.
	(yaml-emit-value!): New primitive wrapper.
	(yaml-emit-anchor!): New primitive wrapper.
	(yaml-emit-alias!): New primitive wrapper.
	(yaml-set-style-1!): New primitive wrapper.
	(yaml-set-string-format-1!): New primitive wrapper.
	(yaml-set-bool-format-1!): New primitive wrapper.
	(yaml-set-int-base!): New primitive wrapper.

2024-01-04  Georgios Athanasiou  <yorgath@gmail.com>

	* cxx/macros.h: Remove file no longer necessary.

	* cxx/emitter.cxx
	(begin_doc): Make it static.
	(begin_map): Make it static.
	(begin_seq): Make it static.
	(emit_alias): Make it static.
	(emit_boolean): Make it static.
	(emit_comment): Make it static.
	(emit_integer): Make it static.
	(emit_key): Make it static.
	(emit_newline): Make it static.
	(emit_null): Make it static.
	(emit_string): Make it static.
	(emit_value): Make it static.
	(emitter_good): Make it static.
	(emitter_string): Make it static.
	(end_doc): Make it static.
	(end_map): Make it static.
	(end_seq): Make it static.
	(init_emitter): Move initialization here.
	(init_emitter_type): Make it static.
	(make_emitter): Make it static.
	(set_bool_format): Make it static.
	(set_indent): Make it static.
	(set_int_base): Make it static.
	(set_map_format): Make it static.
	(set_seq_format): Make it static.
	(set_string_format): Make it static.
	(set_style): Make it static.

	* cxx/reader.cxx
	(init_node_type): Make it static.
	(init_reader): Move initialization here.
	(load_node): Make it static.
	(load_node_from_file): Make it static.
	(load_nodes): Make it static.
	(load_nodes_from_file): Make it static.
	(scalar_value): Make it static.
	(yaml_node_to_alist): Make it static.
	(yaml_node_to_list): Make it static.
	(yaml_node_type): Make it static.

	* cxx/init.cxx (init): Move initialization code to reader.cxx and
	emitter.cxx.


2024-01-03  Georgios Athanasiou  <yorgath@gmail.com>

	First public release
